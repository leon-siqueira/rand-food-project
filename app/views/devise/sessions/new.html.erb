<div id="login" class="container">
  <div class="row justify-content-center w-100">
    <div class="col-md-8 bg-main-yellow p-5 border border-grey rounded-border">
      <h2 class="roboto-font">Log in</h2>
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email", class: "input-pill" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password", class: "input-pill" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Log in", class: 'btn btn-botao mb-3' %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
